import {StyleSheet} from 'react-native';

export const noticeCardStyles = StyleSheet.create({
  wrapper: {
    width: '100%',
  },
  content: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
