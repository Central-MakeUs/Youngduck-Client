import {StyleSheet} from 'react-native';

export const selectStyles = StyleSheet.create({
  container: {
    borderWidth: 1,
  },
  select: {
    position: 'relative',
  },
  options: {
    position: 'absolute',
    top: 0,
  },
});
