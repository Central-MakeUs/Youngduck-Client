import {StyleSheet} from 'react-native';

export const commentItemStyles = StyleSheet.create({
  name: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  label: {
    marginLeft: 12,
  },
});
