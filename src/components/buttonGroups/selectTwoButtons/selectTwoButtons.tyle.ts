import {StyleSheet} from 'react-native';

export const selectTwoButtonStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignItems: 'center',
    paddingLeft: 16,
  },
});
