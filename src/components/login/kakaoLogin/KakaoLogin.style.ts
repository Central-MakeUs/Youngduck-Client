import {StyleSheet} from 'react-native';

const kakaoLoginStyles = StyleSheet.create({
  button: {
    backgroundColor: 'yellow',
    alignItems: 'center',
    paddingVertical: 10,
  },
});

export default kakaoLoginStyles;
