import {StyleSheet} from 'react-native';

const chips = StyleSheet.create({
  Chips1: {
    fontWeight: '400',
    fontSize: 12,
    lineHeight: 18,
  },
  Chips2: {
    fontWeight: '400',
    fontSize: 11,
    lineHeight: 16,
  },
});

export default chips;
