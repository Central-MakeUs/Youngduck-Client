import {StyleSheet} from 'react-native';

export const reviewScreenStyles = StyleSheet.create({
  title: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 24,
    marginBottom: 8,
  },
  bottom: {
    marginBottom: 18,
  },
});
