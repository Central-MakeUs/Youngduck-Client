import {StyleSheet} from 'react-native';

export const reviewScreenStyles = StyleSheet.create({
  title: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 8,
    marginTop: 8,
  },
  bottom: {
    marginBottom: 18,
  },
});
