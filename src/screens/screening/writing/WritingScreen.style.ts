import {StyleSheet} from 'react-native';

export const writingStyles = StyleSheet.create({
  container: {
    marginBottom: 24,
  },
  moreInformation: {
    marginBottom: 2,
  },
  bottom: {
    marginBottom: 48,
  },
});
