import {StyleSheet} from 'react-native';

const lookAroundStyle = StyleSheet.create({
  button: {
    alignItems: 'center',
    paddingVertical: 12,
    borderRadius: 8,
    marginBottom: 12,
  },
});

export default lookAroundStyle;
