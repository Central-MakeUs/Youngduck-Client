import {StyleSheet} from 'react-native';

const manageScreeningScreenStyles = StyleSheet.create({
  menuWrap: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    paddingHorizontal: 16,
  },
  paddingWrap: {
    paddingHorizontal: 16,
  },
  screeningListContainer: {
    flex: 1,
    backgroundColor: '#F5F5F5',
    paddingHorizontal: 16,
    paddingTop: 16,
  },
});

export default manageScreeningScreenStyles;
